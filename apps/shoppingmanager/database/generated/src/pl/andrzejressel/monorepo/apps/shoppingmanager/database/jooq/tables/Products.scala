/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables

import org.jooq.Condition
import org.jooq.Field
import org.jooq.ForeignKey
import org.jooq.InverseForeignKey
import org.jooq.Name
import org.jooq.Path
import org.jooq.PlainSQL
import org.jooq.Record
import org.jooq.SQL
import org.jooq.Schema
import org.jooq.Stringly
import org.jooq.Table
import org.jooq.TableField
import org.jooq.TableLike
import org.jooq.TableOptions
import org.jooq.UniqueKey
import org.jooq.impl.DSL
import org.jooq.impl.Internal
import org.jooq.impl.SQLDataType
import org.jooq.impl.TableImpl
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.DefaultSchema
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.Keys
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Categories.CategoriesPath
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.StoreProducts.StoreProductsPath
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.records.ProductsRecord

import java.lang.Boolean
import java.lang.Class
import java.lang.String
import java.util.Arrays
import java.util.Collection
import java.util.List
import scala.Array

object Products {

  /** The reference instance of <code>products</code>
    */
  val PRODUCTS = new Products

  /** A subtype implementing {@link Path} for simplified path-based joins.
    */
  class ProductsPath(
      path: Table[? <: Record],
      childPath: ForeignKey[? <: Record, ProductsRecord],
      parentPath: InverseForeignKey[? <: Record, ProductsRecord]
  ) extends Products(path, childPath, parentPath)
      with Path[ProductsRecord]
}

/** This class is generated by jOOQ.
  */
class Products(
    alias: Name,
    path: Table[? <: Record],
    childPath: ForeignKey[? <: Record, ProductsRecord],
    parentPath: InverseForeignKey[? <: Record, ProductsRecord],
    aliased: Table[ProductsRecord],
    parameters: Array[Field[?]],
    where: Condition
) extends TableImpl[ProductsRecord](
      alias,
      DefaultSchema.DEFAULT_SCHEMA,
      path,
      childPath,
      parentPath,
      aliased,
      parameters,
      DSL.comment(""),
      TableOptions.table,
      where
    ) {

  /** The class holding records for this type
    */
  override def getRecordType: Class[ProductsRecord] = classOf[ProductsRecord]

  /** The column <code>products.id</code>.
    */
  val ID: TableField[ProductsRecord, String] =
    createField(DSL.name("id"), SQLDataType.CLOB, "")

  /** The column <code>products.name</code>.
    */
  val NAME: TableField[ProductsRecord, String] =
    createField(DSL.name("name"), SQLDataType.CLOB.nullable(false), "")

  /** The column <code>products.category_id</code>.
    */
  val CATEGORY_ID: TableField[ProductsRecord, String] =
    createField(DSL.name("category_id"), SQLDataType.CLOB, "")

  private def this(alias: Name, aliased: Table[ProductsRecord]) =
    this(alias, null, null, null, aliased, null, null)
  private def this(
      alias: Name,
      aliased: Table[ProductsRecord],
      where: Condition
  ) = this(alias, null, null, null, aliased, null, where)

  /** Create an aliased <code>products</code> table reference
    */
  def this(alias: String) = this(
    DSL.name(alias),
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Products.PRODUCTS
  )

  /** Create an aliased <code>products</code> table reference
    */
  def this(alias: Name) = this(
    alias,
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Products.PRODUCTS
  )

  /** Create a <code>products</code> table reference
    */
  def this() = this(DSL.name("products"), null)

  def this(
      path: Table[? <: Record],
      childPath: ForeignKey[? <: Record, ProductsRecord],
      parentPath: InverseForeignKey[? <: Record, ProductsRecord]
  ) = this(
    Internal.createPathAlias(path, childPath, parentPath),
    path,
    childPath,
    parentPath,
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Products.PRODUCTS,
    null,
    null
  )

  override def getSchema: Schema =
    if super.aliased() then null else DefaultSchema.DEFAULT_SCHEMA

  override def getPrimaryKey: UniqueKey[ProductsRecord] =
    Keys.PRODUCTS__PK_PRODUCTS

  override def getReferences: List[ForeignKey[ProductsRecord, ?]] =
    Arrays.asList[ForeignKey[ProductsRecord, ?]](
      Keys.PRODUCTS__FK_PRODUCTS_PK_CATEGORIES
    )

  /** Get the implicit join path to the <code>categories</code> table.
    */
  lazy val categories: CategoriesPath = {
    new CategoriesPath(this, Keys.PRODUCTS__FK_PRODUCTS_PK_CATEGORIES, null)
  }

  /** Get the implicit to-many join path to the <code>store_products</code>
    * table
    */
  lazy val storeProducts: StoreProductsPath = {
    new StoreProductsPath(
      this,
      null,
      Keys.STORE_PRODUCTS__FK_STORE_PRODUCTS_PK_PRODUCTS.getInverseKey()
    )
  }
  override def as(alias: String): Products = new Products(DSL.name(alias), this)
  override def as(alias: Name): Products = new Products(alias, this)
  override def as(alias: Table[?]): Products =
    new Products(alias.getQualifiedName(), this)

  /** Rename this table
    */
  override def rename(name: String): Products =
    new Products(DSL.name(name), null)

  /** Rename this table
    */
  override def rename(name: Name): Products = new Products(name, null)

  /** Rename this table
    */
  override def rename(name: Table[?]): Products =
    new Products(name.getQualifiedName(), null)

  /** Create an inline derived table from this table
    */
  override def where(condition: Condition): Products = new Products(
    getQualifiedName(),
    if super.aliased() then this else null,
    Internal.condition(this, condition)
  )

  /** Create an inline derived table from this table
    */
  override def where(conditions: Collection[? <: Condition]): Products = where(
    DSL.and(conditions)
  )

  /** Create an inline derived table from this table
    */
  override def where(conditions: Condition*): Products = where(
    DSL.and(conditions*)
  )

  /** Create an inline derived table from this table
    */
  override def where(condition: Field[Boolean]): Products = where(
    DSL.condition(condition)
  )

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(condition: SQL): Products = where(
    DSL.condition(condition)
  )

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(@Stringly.SQL condition: String): Products =
    where(DSL.condition(condition))

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(
      @Stringly.SQL condition: String,
      binds: AnyRef*
  ): Products = where(DSL.condition(condition, binds*))

  /** Create an inline derived table from this table
    */
  override def whereExists(select: TableLike[?]): Products = where(
    DSL.exists(select)
  )

  /** Create an inline derived table from this table
    */
  override def whereNotExists(select: TableLike[?]): Products = where(
    DSL.notExists(select)
  )
}
