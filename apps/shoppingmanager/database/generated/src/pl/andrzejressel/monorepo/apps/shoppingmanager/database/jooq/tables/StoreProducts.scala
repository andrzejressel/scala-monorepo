/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables

import org.jooq.Condition
import org.jooq.Field
import org.jooq.ForeignKey
import org.jooq.InverseForeignKey
import org.jooq.Name
import org.jooq.Path
import org.jooq.PlainSQL
import org.jooq.Record
import org.jooq.SQL
import org.jooq.Schema
import org.jooq.Stringly
import org.jooq.Table
import org.jooq.TableField
import org.jooq.TableLike
import org.jooq.TableOptions
import org.jooq.UniqueKey
import org.jooq.impl.DSL
import org.jooq.impl.Internal
import org.jooq.impl.SQLDataType
import org.jooq.impl.TableImpl
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.DefaultSchema
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.Keys
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Products.ProductsPath
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.TransactionItems.TransactionItemsPath
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.records.StoreProductsRecord

import java.lang.Boolean
import java.lang.Class
import java.lang.String
import java.util.Arrays
import java.util.Collection
import java.util.List
import scala.Array

object StoreProducts {

  /** The reference instance of <code>store_products</code>
    */
  val STORE_PRODUCTS = new StoreProducts

  /** A subtype implementing {@link Path} for simplified path-based joins.
    */
  class StoreProductsPath(
      path: Table[? <: Record],
      childPath: ForeignKey[? <: Record, StoreProductsRecord],
      parentPath: InverseForeignKey[? <: Record, StoreProductsRecord]
  ) extends StoreProducts(path, childPath, parentPath)
      with Path[StoreProductsRecord]
}

/** This class is generated by jOOQ.
  */
class StoreProducts(
    alias: Name,
    path: Table[? <: Record],
    childPath: ForeignKey[? <: Record, StoreProductsRecord],
    parentPath: InverseForeignKey[? <: Record, StoreProductsRecord],
    aliased: Table[StoreProductsRecord],
    parameters: Array[Field[?]],
    where: Condition
) extends TableImpl[StoreProductsRecord](
      alias,
      DefaultSchema.DEFAULT_SCHEMA,
      path,
      childPath,
      parentPath,
      aliased,
      parameters,
      DSL.comment(""),
      TableOptions.table,
      where
    ) {

  /** The class holding records for this type
    */
  override def getRecordType: Class[StoreProductsRecord] =
    classOf[StoreProductsRecord]

  /** The column <code>store_products.store_type</code>.
    */
  val STORE_TYPE: TableField[StoreProductsRecord, String] =
    createField(DSL.name("store_type"), SQLDataType.CLOB.nullable(false), "")

  /** The column <code>store_products.id</code>.
    */
  val ID: TableField[StoreProductsRecord, String] =
    createField(DSL.name("id"), SQLDataType.CLOB.nullable(false), "")

  /** The column <code>store_products.name</code>.
    */
  val NAME: TableField[StoreProductsRecord, String] =
    createField(DSL.name("name"), SQLDataType.CLOB.nullable(false), "")

  /** The column <code>store_products.product_id</code>.
    */
  val PRODUCT_ID: TableField[StoreProductsRecord, String] =
    createField(DSL.name("product_id"), SQLDataType.CLOB.nullable(false), "")

  private def this(alias: Name, aliased: Table[StoreProductsRecord]) =
    this(alias, null, null, null, aliased, null, null)
  private def this(
      alias: Name,
      aliased: Table[StoreProductsRecord],
      where: Condition
  ) = this(alias, null, null, null, aliased, null, where)

  /** Create an aliased <code>store_products</code> table reference
    */
  def this(alias: String) = this(
    DSL.name(alias),
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.StoreProducts.STORE_PRODUCTS
  )

  /** Create an aliased <code>store_products</code> table reference
    */
  def this(alias: Name) = this(
    alias,
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.StoreProducts.STORE_PRODUCTS
  )

  /** Create a <code>store_products</code> table reference
    */
  def this() = this(DSL.name("store_products"), null)

  def this(
      path: Table[? <: Record],
      childPath: ForeignKey[? <: Record, StoreProductsRecord],
      parentPath: InverseForeignKey[? <: Record, StoreProductsRecord]
  ) = this(
    Internal.createPathAlias(path, childPath, parentPath),
    path,
    childPath,
    parentPath,
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.StoreProducts.STORE_PRODUCTS,
    null,
    null
  )

  override def getSchema: Schema =
    if super.aliased() then null else DefaultSchema.DEFAULT_SCHEMA

  override def getPrimaryKey: UniqueKey[StoreProductsRecord] =
    Keys.STORE_PRODUCTS__PK_STORE_PRODUCTS

  override def getReferences: List[ForeignKey[StoreProductsRecord, ?]] =
    Arrays.asList[ForeignKey[StoreProductsRecord, ?]](
      Keys.STORE_PRODUCTS__FK_STORE_PRODUCTS_PK_PRODUCTS
    )

  /** Get the implicit join path to the <code>products</code> table.
    */
  lazy val products: ProductsPath = {
    new ProductsPath(
      this,
      Keys.STORE_PRODUCTS__FK_STORE_PRODUCTS_PK_PRODUCTS,
      null
    )
  }

  /** Get the implicit to-many join path to the <code>transaction_items</code>
    * table
    */
  lazy val transactionItems: TransactionItemsPath = {
    new TransactionItemsPath(
      this,
      null,
      Keys.TRANSACTION_ITEMS__FK_TRANSACTION_ITEMS_PK_STORE_PRODUCTS
        .getInverseKey()
    )
  }
  override def as(alias: String): StoreProducts =
    new StoreProducts(DSL.name(alias), this)
  override def as(alias: Name): StoreProducts = new StoreProducts(alias, this)
  override def as(alias: Table[?]): StoreProducts =
    new StoreProducts(alias.getQualifiedName(), this)

  /** Rename this table
    */
  override def rename(name: String): StoreProducts =
    new StoreProducts(DSL.name(name), null)

  /** Rename this table
    */
  override def rename(name: Name): StoreProducts = new StoreProducts(name, null)

  /** Rename this table
    */
  override def rename(name: Table[?]): StoreProducts =
    new StoreProducts(name.getQualifiedName(), null)

  /** Create an inline derived table from this table
    */
  override def where(condition: Condition): StoreProducts = new StoreProducts(
    getQualifiedName(),
    if super.aliased() then this else null,
    Internal.condition(this, condition)
  )

  /** Create an inline derived table from this table
    */
  override def where(conditions: Collection[? <: Condition]): StoreProducts =
    where(DSL.and(conditions))

  /** Create an inline derived table from this table
    */
  override def where(conditions: Condition*): StoreProducts = where(
    DSL.and(conditions*)
  )

  /** Create an inline derived table from this table
    */
  override def where(condition: Field[Boolean]): StoreProducts = where(
    DSL.condition(condition)
  )

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(condition: SQL): StoreProducts = where(
    DSL.condition(condition)
  )

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(@Stringly.SQL condition: String): StoreProducts =
    where(DSL.condition(condition))

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(
      @Stringly.SQL condition: String,
      binds: AnyRef*
  ): StoreProducts = where(DSL.condition(condition, binds*))

  /** Create an inline derived table from this table
    */
  override def whereExists(select: TableLike[?]): StoreProducts = where(
    DSL.exists(select)
  )

  /** Create an inline derived table from this table
    */
  override def whereNotExists(select: TableLike[?]): StoreProducts = where(
    DSL.notExists(select)
  )
}
