/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.pojos

import java.io.Serializable
import java.lang.String
import java.lang.StringBuilder

/** This class is generated by jOOQ.
  */
case class Transactions(
    val id: String,
    val date: String
) extends Serializable {

  def this(value: Transactions) = this(
    value.id,
    value.date
  )

  /** Getter for <code>transactions.id</code>.
    */
  def getId: String = this.id

  /** Getter for <code>transactions.date</code>.
    */
  def getDate: String = this.date

  override def equals(obj: Any): scala.Boolean = {
    if this eq obj.asInstanceOf[AnyRef] then return true
    if obj == null then return false
    if getClass() != obj.getClass() then return false
    val other = obj.asInstanceOf[Transactions]
    if this.id == null then {
      if other.id != null then return false
    } else if !this.id.equals(other.id) then return false
    if this.date == null then {
      if other.date != null then return false
    } else if !this.date.equals(other.date) then return false
    true
  }

  override def hashCode: Int = {
    val prime = 31
    var result = 1
    result = prime * result + (if this.id == null then 0 else this.id.hashCode)
    result =
      prime * result + (if this.date == null then 0 else this.date.hashCode)
    return result
  }

  override def toString: String = {
    val sb = new StringBuilder("Transactions (")

    sb.append(id)
    sb.append(", ").append(date)

    sb.append(")")
    sb.toString
  }
}
