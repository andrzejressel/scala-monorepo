/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.pojos

import java.io.Serializable
import java.lang.String
import java.lang.StringBuilder

/** This class is generated by jOOQ.
  */
case class StoreProducts(
    val storeType: String,
    val id: String,
    val name: String,
    val productId: String
) extends Serializable {

  def this(value: StoreProducts) = this(
    value.storeType,
    value.id,
    value.name,
    value.productId
  )

  /** Getter for <code>store_products.store_type</code>.
    */
  def getStoreType: String = this.storeType

  /** Getter for <code>store_products.id</code>.
    */
  def getId: String = this.id

  /** Getter for <code>store_products.name</code>.
    */
  def getName: String = this.name

  /** Getter for <code>store_products.product_id</code>.
    */
  def getProductId: String = this.productId

  override def equals(obj: Any): scala.Boolean = {
    if this eq obj.asInstanceOf[AnyRef] then return true
    if obj == null then return false
    if getClass() != obj.getClass() then return false
    val other = obj.asInstanceOf[StoreProducts]
    if this.storeType == null then {
      if other.storeType != null then return false
    } else if !this.storeType.equals(other.storeType) then return false
    if this.id == null then {
      if other.id != null then return false
    } else if !this.id.equals(other.id) then return false
    if this.name == null then {
      if other.name != null then return false
    } else if !this.name.equals(other.name) then return false
    if this.productId == null then {
      if other.productId != null then return false
    } else if !this.productId.equals(other.productId) then return false
    true
  }

  override def hashCode: Int = {
    val prime = 31
    var result = 1
    result = prime * result + (if this.storeType == null then 0
                               else this.storeType.hashCode)
    result = prime * result + (if this.id == null then 0 else this.id.hashCode)
    result =
      prime * result + (if this.name == null then 0 else this.name.hashCode)
    result = prime * result + (if this.productId == null then 0
                               else this.productId.hashCode)
    return result
  }

  override def toString: String = {
    val sb = new StringBuilder("StoreProducts (")

    sb.append(storeType)
    sb.append(", ").append(id)
    sb.append(", ").append(name)
    sb.append(", ").append(productId)

    sb.append(")")
    sb.toString
  }
}
