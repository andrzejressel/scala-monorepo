/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables

import org.jooq.Condition
import org.jooq.Field
import org.jooq.ForeignKey
import org.jooq.InverseForeignKey
import org.jooq.Name
import org.jooq.Path
import org.jooq.PlainSQL
import org.jooq.Record
import org.jooq.SQL
import org.jooq.Schema
import org.jooq.Stringly
import org.jooq.Table
import org.jooq.TableField
import org.jooq.TableLike
import org.jooq.TableOptions
import org.jooq.UniqueKey
import org.jooq.impl.DSL
import org.jooq.impl.Internal
import org.jooq.impl.SQLDataType
import org.jooq.impl.TableImpl
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.DefaultSchema
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.Keys
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Categories.CategoriesPath
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Products.ProductsPath
import pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.records.CategoriesRecord

import java.lang.Boolean
import java.lang.Class
import java.lang.String
import java.util.Arrays
import java.util.Collection
import java.util.List
import scala.Array

object Categories {

  /** The reference instance of <code>categories</code>
    */
  val CATEGORIES = new Categories

  /** A subtype implementing {@link Path} for simplified path-based joins.
    */
  class CategoriesPath(
      path: Table[? <: Record],
      childPath: ForeignKey[? <: Record, CategoriesRecord],
      parentPath: InverseForeignKey[? <: Record, CategoriesRecord]
  ) extends Categories(path, childPath, parentPath)
      with Path[CategoriesRecord]
}

/** This class is generated by jOOQ.
  */
class Categories(
    alias: Name,
    path: Table[? <: Record],
    childPath: ForeignKey[? <: Record, CategoriesRecord],
    parentPath: InverseForeignKey[? <: Record, CategoriesRecord],
    aliased: Table[CategoriesRecord],
    parameters: Array[Field[?]],
    where: Condition
) extends TableImpl[CategoriesRecord](
      alias,
      DefaultSchema.DEFAULT_SCHEMA,
      path,
      childPath,
      parentPath,
      aliased,
      parameters,
      DSL.comment(""),
      TableOptions.table,
      where
    ) {

  /** The class holding records for this type
    */
  override def getRecordType: Class[CategoriesRecord] =
    classOf[CategoriesRecord]

  /** The column <code>categories.id</code>.
    */
  val ID: TableField[CategoriesRecord, String] =
    createField(DSL.name("id"), SQLDataType.CLOB, "")

  /** The column <code>categories.name</code>.
    */
  val NAME: TableField[CategoriesRecord, String] =
    createField(DSL.name("name"), SQLDataType.CLOB.nullable(false), "")

  /** The column <code>categories.parent_id</code>.
    */
  val PARENT_ID: TableField[CategoriesRecord, String] =
    createField(DSL.name("parent_id"), SQLDataType.CLOB, "")

  private def this(alias: Name, aliased: Table[CategoriesRecord]) =
    this(alias, null, null, null, aliased, null, null)
  private def this(
      alias: Name,
      aliased: Table[CategoriesRecord],
      where: Condition
  ) = this(alias, null, null, null, aliased, null, where)

  /** Create an aliased <code>categories</code> table reference
    */
  def this(alias: String) = this(
    DSL.name(alias),
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Categories.CATEGORIES
  )

  /** Create an aliased <code>categories</code> table reference
    */
  def this(alias: Name) = this(
    alias,
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Categories.CATEGORIES
  )

  /** Create a <code>categories</code> table reference
    */
  def this() = this(DSL.name("categories"), null)

  def this(
      path: Table[? <: Record],
      childPath: ForeignKey[? <: Record, CategoriesRecord],
      parentPath: InverseForeignKey[? <: Record, CategoriesRecord]
  ) = this(
    Internal.createPathAlias(path, childPath, parentPath),
    path,
    childPath,
    parentPath,
    pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.Categories.CATEGORIES,
    null,
    null
  )

  override def getSchema: Schema =
    if super.aliased() then null else DefaultSchema.DEFAULT_SCHEMA

  override def getPrimaryKey: UniqueKey[CategoriesRecord] =
    Keys.CATEGORIES__PK_CATEGORIES

  override def getReferences: List[ForeignKey[CategoriesRecord, ?]] =
    Arrays.asList[ForeignKey[CategoriesRecord, ?]](
      Keys.CATEGORIES__FK_CATEGORIES_PK_CATEGORIES
    )

  /** Get the implicit join path to the <code>categories</code> table.
    */
  lazy val categories: CategoriesPath = {
    new CategoriesPath(this, Keys.CATEGORIES__FK_CATEGORIES_PK_CATEGORIES, null)
  }

  /** Get the implicit to-many join path to the <code>products</code> table
    */
  lazy val products: ProductsPath = {
    new ProductsPath(
      this,
      null,
      Keys.PRODUCTS__FK_PRODUCTS_PK_CATEGORIES.getInverseKey()
    )
  }
  override def as(alias: String): Categories =
    new Categories(DSL.name(alias), this)
  override def as(alias: Name): Categories = new Categories(alias, this)
  override def as(alias: Table[?]): Categories =
    new Categories(alias.getQualifiedName(), this)

  /** Rename this table
    */
  override def rename(name: String): Categories =
    new Categories(DSL.name(name), null)

  /** Rename this table
    */
  override def rename(name: Name): Categories = new Categories(name, null)

  /** Rename this table
    */
  override def rename(name: Table[?]): Categories =
    new Categories(name.getQualifiedName(), null)

  /** Create an inline derived table from this table
    */
  override def where(condition: Condition): Categories = new Categories(
    getQualifiedName(),
    if super.aliased() then this else null,
    Internal.condition(this, condition)
  )

  /** Create an inline derived table from this table
    */
  override def where(conditions: Collection[? <: Condition]): Categories =
    where(DSL.and(conditions))

  /** Create an inline derived table from this table
    */
  override def where(conditions: Condition*): Categories = where(
    DSL.and(conditions*)
  )

  /** Create an inline derived table from this table
    */
  override def where(condition: Field[Boolean]): Categories = where(
    DSL.condition(condition)
  )

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(condition: SQL): Categories = where(
    DSL.condition(condition)
  )

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(@Stringly.SQL condition: String): Categories =
    where(DSL.condition(condition))

  /** Create an inline derived table from this table
    */
  @PlainSQL override def where(
      @Stringly.SQL condition: String,
      binds: AnyRef*
  ): Categories = where(DSL.condition(condition, binds*))

  /** Create an inline derived table from this table
    */
  override def whereExists(select: TableLike[?]): Categories = where(
    DSL.exists(select)
  )

  /** Create an inline derived table from this table
    */
  override def whereNotExists(select: TableLike[?]): Categories = where(
    DSL.notExists(select)
  )
}
