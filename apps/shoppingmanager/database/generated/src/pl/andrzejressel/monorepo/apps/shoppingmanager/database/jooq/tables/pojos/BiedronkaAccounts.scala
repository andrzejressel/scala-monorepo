/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.pojos

import java.io.Serializable
import java.lang.String
import java.lang.StringBuilder

/** This class is generated by jOOQ.
  */
case class BiedronkaAccounts(
    val id: String,
    val refreshToken: String
) extends Serializable {

  def this(value: BiedronkaAccounts) = this(
    value.id,
    value.refreshToken
  )

  /** Getter for <code>biedronka_accounts.id</code>.
    */
  def getId: String = this.id

  /** Getter for <code>biedronka_accounts.refresh_token</code>.
    */
  def getRefreshToken: String = this.refreshToken

  override def equals(obj: Any): scala.Boolean = {
    if this eq obj.asInstanceOf[AnyRef] then return true
    if obj == null then return false
    if getClass() != obj.getClass() then return false
    val other = obj.asInstanceOf[BiedronkaAccounts]
    if this.id == null then {
      if other.id != null then return false
    } else if !this.id.equals(other.id) then return false
    if this.refreshToken == null then {
      if other.refreshToken != null then return false
    } else if !this.refreshToken.equals(other.refreshToken) then return false
    true
  }

  override def hashCode: Int = {
    val prime = 31
    var result = 1
    result = prime * result + (if this.id == null then 0 else this.id.hashCode)
    result = prime * result + (if this.refreshToken == null then 0
                               else this.refreshToken.hashCode)
    return result
  }

  override def toString: String = {
    val sb = new StringBuilder("BiedronkaAccounts (")

    sb.append(id)
    sb.append(", ").append(refreshToken)

    sb.append(")")
    sb.toString
  }
}
