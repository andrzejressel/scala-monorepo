/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.pojos

import java.io.Serializable
import java.lang.String
import java.lang.StringBuilder

/** This class is generated by jOOQ.
  */
case class Products(
    val id: String,
    val name: String,
    val categoryId: String
) extends Serializable {

  def this(value: Products) = this(
    value.id,
    value.name,
    value.categoryId
  )

  /** Getter for <code>products.id</code>.
    */
  def getId: String = this.id

  /** Getter for <code>products.name</code>.
    */
  def getName: String = this.name

  /** Getter for <code>products.category_id</code>.
    */
  def getCategoryId: String = this.categoryId

  override def equals(obj: Any): scala.Boolean = {
    if this eq obj.asInstanceOf[AnyRef] then return true
    if obj == null then return false
    if getClass() != obj.getClass() then return false
    val other = obj.asInstanceOf[Products]
    if this.id == null then {
      if other.id != null then return false
    } else if !this.id.equals(other.id) then return false
    if this.name == null then {
      if other.name != null then return false
    } else if !this.name.equals(other.name) then return false
    if this.categoryId == null then {
      if other.categoryId != null then return false
    } else if !this.categoryId.equals(other.categoryId) then return false
    true
  }

  override def hashCode: Int = {
    val prime = 31
    var result = 1
    result = prime * result + (if this.id == null then 0 else this.id.hashCode)
    result =
      prime * result + (if this.name == null then 0 else this.name.hashCode)
    result = prime * result + (if this.categoryId == null then 0
                               else this.categoryId.hashCode)
    return result
  }

  override def toString: String = {
    val sb = new StringBuilder("Products (")

    sb.append(id)
    sb.append(", ").append(name)
    sb.append(", ").append(categoryId)

    sb.append(")")
    sb.toString
  }
}
