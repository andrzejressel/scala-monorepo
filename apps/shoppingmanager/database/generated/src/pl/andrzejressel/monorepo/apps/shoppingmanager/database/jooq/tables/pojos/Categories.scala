/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.pojos

import java.io.Serializable
import java.lang.String
import java.lang.StringBuilder

/** This class is generated by jOOQ.
  */
case class Categories(
    val id: String,
    val name: String,
    val parentId: String
) extends Serializable {

  def this(value: Categories) = this(
    value.id,
    value.name,
    value.parentId
  )

  /** Getter for <code>categories.id</code>.
    */
  def getId: String = this.id

  /** Getter for <code>categories.name</code>.
    */
  def getName: String = this.name

  /** Getter for <code>categories.parent_id</code>.
    */
  def getParentId: String = this.parentId

  override def equals(obj: Any): scala.Boolean = {
    if this eq obj.asInstanceOf[AnyRef] then return true
    if obj == null then return false
    if getClass() != obj.getClass() then return false
    val other = obj.asInstanceOf[Categories]
    if this.id == null then {
      if other.id != null then return false
    } else if !this.id.equals(other.id) then return false
    if this.name == null then {
      if other.name != null then return false
    } else if !this.name.equals(other.name) then return false
    if this.parentId == null then {
      if other.parentId != null then return false
    } else if !this.parentId.equals(other.parentId) then return false
    true
  }

  override def hashCode: Int = {
    val prime = 31
    var result = 1
    result = prime * result + (if this.id == null then 0 else this.id.hashCode)
    result =
      prime * result + (if this.name == null then 0 else this.name.hashCode)
    result = prime * result + (if this.parentId == null then 0
                               else this.parentId.hashCode)
    return result
  }

  override def toString: String = {
    val sb = new StringBuilder("Categories (")

    sb.append(id)
    sb.append(", ").append(name)
    sb.append(", ").append(parentId)

    sb.append(")")
    sb.toString
  }
}
