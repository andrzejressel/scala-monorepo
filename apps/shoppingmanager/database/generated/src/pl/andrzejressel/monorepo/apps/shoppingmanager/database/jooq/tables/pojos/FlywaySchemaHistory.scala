/*
 * This file is generated by jOOQ.
 */
package pl.andrzejressel.monorepo.apps.shoppingmanager.database.jooq.tables.pojos

import java.io.Serializable
import java.lang.Boolean
import java.lang.Integer
import java.lang.String
import java.lang.StringBuilder

/** This class is generated by jOOQ.
  */
case class FlywaySchemaHistory(
    val installedRank: Integer,
    val version: String,
    val description: String,
    val `type`: String,
    val script: String,
    val checksum: Integer,
    val installedBy: String,
    val installedOn: String,
    val executionTime: Integer,
    val success: Boolean
) extends Serializable {

  def this(value: FlywaySchemaHistory) = this(
    value.installedRank,
    value.version,
    value.description,
    value.`type`,
    value.script,
    value.checksum,
    value.installedBy,
    value.installedOn,
    value.executionTime,
    value.success
  )

  /** Getter for <code>flyway_schema_history.installed_rank</code>.
    */
  def getInstalledRank: Integer = this.installedRank

  /** Getter for <code>flyway_schema_history.version</code>.
    */
  def getVersion: String = this.version

  /** Getter for <code>flyway_schema_history.description</code>.
    */
  def getDescription: String = this.description

  /** Getter for <code>flyway_schema_history.type</code>.
    */
  def getType: String = this.`type`

  /** Getter for <code>flyway_schema_history.script</code>.
    */
  def getScript: String = this.script

  /** Getter for <code>flyway_schema_history.checksum</code>.
    */
  def getChecksum: Integer = this.checksum

  /** Getter for <code>flyway_schema_history.installed_by</code>.
    */
  def getInstalledBy: String = this.installedBy

  /** Getter for <code>flyway_schema_history.installed_on</code>.
    */
  def getInstalledOn: String = this.installedOn

  /** Getter for <code>flyway_schema_history.execution_time</code>.
    */
  def getExecutionTime: Integer = this.executionTime

  /** Getter for <code>flyway_schema_history.success</code>.
    */
  def getSuccess: Boolean = this.success

  override def equals(obj: Any): scala.Boolean = {
    if this eq obj.asInstanceOf[AnyRef] then return true
    if obj == null then return false
    if getClass() != obj.getClass() then return false
    val other = obj.asInstanceOf[FlywaySchemaHistory]
    if this.installedRank == null then {
      if other.installedRank != null then return false
    } else if !this.installedRank.equals(other.installedRank) then return false
    if this.version == null then {
      if other.version != null then return false
    } else if !this.version.equals(other.version) then return false
    if this.description == null then {
      if other.description != null then return false
    } else if !this.description.equals(other.description) then return false
    if this.`type` == null then {
      if other.`type` != null then return false
    } else if !this.`type`.equals(other.`type`) then return false
    if this.script == null then {
      if other.script != null then return false
    } else if !this.script.equals(other.script) then return false
    if this.checksum == null then {
      if other.checksum != null then return false
    } else if !this.checksum.equals(other.checksum) then return false
    if this.installedBy == null then {
      if other.installedBy != null then return false
    } else if !this.installedBy.equals(other.installedBy) then return false
    if this.installedOn == null then {
      if other.installedOn != null then return false
    } else if !this.installedOn.equals(other.installedOn) then return false
    if this.executionTime == null then {
      if other.executionTime != null then return false
    } else if !this.executionTime.equals(other.executionTime) then return false
    if this.success == null then {
      if other.success != null then return false
    } else if !this.success.equals(other.success) then return false
    true
  }

  override def hashCode: Int = {
    val prime = 31
    var result = 1
    result = prime * result + (if this.installedRank == null then 0
                               else this.installedRank.hashCode)
    result = prime * result + (if this.version == null then 0
                               else this.version.hashCode)
    result = prime * result + (if this.description == null then 0
                               else this.description.hashCode)
    result =
      prime * result + (if this.`type` == null then 0 else this.`type`.hashCode)
    result =
      prime * result + (if this.script == null then 0 else this.script.hashCode)
    result = prime * result + (if this.checksum == null then 0
                               else this.checksum.hashCode)
    result = prime * result + (if this.installedBy == null then 0
                               else this.installedBy.hashCode)
    result = prime * result + (if this.installedOn == null then 0
                               else this.installedOn.hashCode)
    result = prime * result + (if this.executionTime == null then 0
                               else this.executionTime.hashCode)
    result = prime * result + (if this.success == null then 0
                               else this.success.hashCode)
    return result
  }

  override def toString: String = {
    val sb = new StringBuilder("FlywaySchemaHistory (")

    sb.append(installedRank)
    sb.append(", ").append(version)
    sb.append(", ").append(description)
    sb.append(", ").append(`type`)
    sb.append(", ").append(script)
    sb.append(", ").append(checksum)
    sb.append(", ").append(installedBy)
    sb.append(", ").append(installedOn)
    sb.append(", ").append(executionTime)
    sb.append(", ").append(success)

    sb.append(")")
    sb.toString
  }
}
