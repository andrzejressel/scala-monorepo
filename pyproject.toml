[project]
name = "scala-monorepo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Andrzej Ressel", email = "github@andrzejressel.pl" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
scala-monorepo = "scala_monorepo:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.59",
]
